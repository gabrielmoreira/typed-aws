import DataLakeSettings_ from './DataLakeSettings';
import Permissions_ from './Permissions';
import Resource_ from './Resource';

export namespace LakeFormation {
  export const DataLakeSettings = DataLakeSettings_;
  export const Permissions = Permissions_;
  export const Resource = Resource_;
}

export default LakeFormation;
